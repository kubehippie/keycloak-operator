---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: groupmembershipprotocolmappers.openid.keycloak-operator.webhippie.de
spec:
  group: openid.keycloak-operator.webhippie.de
  names:
    kind: GroupMembershipProtocolMapper
    listKind: GroupMembershipProtocolMapperList
    plural: groupmembershipprotocolmappers
    singular: groupmembershipprotocolmapper
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.clientRef.name
      name: Client
      type: string
    - jsonPath: .spec.name
      name: Name
      type: string
    - jsonPath: .status.keycloakID
      name: KeycloakID
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: GroupMembershipProtocolMapper is the Schema for the groupmembershipprotocolmappers
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of GroupMembershipProtocolMapper
            properties:
              addToAccessToken:
                description: addToAccessToken controls whether the claim is included
                  in access tokens.
                type: boolean
              addToIDToken:
                description: addToIDToken controls whether the claim is included in
                  ID tokens.
                type: boolean
              addToTokenIntrospection:
                description: addToTokenIntrospection controls whether the claim is
                  included in token introspection responses.
                type: boolean
              addToUserInfo:
                description: addToUserInfo controls whether the claim is included
                  in the userinfo response.
                type: boolean
              claimName:
                description: claimName is the token claim that receives the group
                  membership data.
                type: string
              clientRef:
                description: clientRef references the OpenIDClient this protocol mapper
                  is attached to.
                properties:
                  kind:
                    default: OpenIDClient
                    description: Kind specifies the kind of the referenced client
                      resource.
                    enum:
                    - OpenIDClient
                    type: string
                  name:
                    description: Name specifies the name of the OpenIDClient resource.
                    type: string
                  namespace:
                    description: Namespace specifies the namespace of the OpenIDClient
                      resource.
                    type: string
                required:
                - name
                type: object
              fullPath:
                description: fullPath controls whether full group paths are emitted
                  instead of simple names.
                type: boolean
              name:
                description: name is the mapper's name as shown in the Keycloak admin
                  console.
                type: string
            required:
            - claimName
            - clientRef
            - name
            type: object
          status:
            description: status defines the observed state of GroupMembershipProtocolMapper
            properties:
              conditions:
                description: |-
                  conditions represent the current state of the GroupMembershipProtocolMapper resource.
                  Each condition has a unique type and reflects the status of a specific aspect of the resource.

                  Standard condition types include:
                  - "Available": the resource is fully functional
                  - "Progressing": the resource is being created or updated
                  - "Degraded": the resource failed to reach or maintain its desired state

                  The status of each condition is one of True, False, or Unknown.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              keycloakID:
                description: keycloakID is the UUID assigned by Keycloak for this
                  protocol mapper.
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
