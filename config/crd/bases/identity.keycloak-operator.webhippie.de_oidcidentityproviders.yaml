---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: oidcidentityproviders.identity.keycloak-operator.webhippie.de
spec:
  group: identity.keycloak-operator.webhippie.de
  names:
    kind: OIDCIdentityProvider
    listKind: OIDCIdentityProviderList
    plural: oidcidentityproviders
    singular: oidcidentityprovider
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.alias
      name: Alias
      type: string
    - jsonPath: .spec.enabled
      name: Enabled
      type: boolean
    - jsonPath: .status.keycloakID
      name: KeycloakID
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: OIDCIdentityProvider is the Schema for the oidcidentityproviders
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of OIDCIdentityProvider
            properties:
              alias:
                description: |-
                  alias is the unique identifier of the identity provider within the realm.
                  This field is immutable after creation.
                type: string
              authorizationURL:
                description: authorizationURL is the OIDC authorization endpoint.
                type: string
              backchannelSupported:
                description: |-
                  backchannelSupported indicates whether the external provider supports
                  backchannel logout.
                type: boolean
              clientID:
                description: clientID is the OIDC client identifier registered with
                  the external provider.
                type: string
              clientSecret:
                description: clientSecret references a secret or inline value holding
                  the OIDC client secret.
                properties:
                  secretKeyRef:
                    description: SecretKeyRef selects a key of a Kubernetes Secret.
                    properties:
                      key:
                        description: Key is the key of the Secret to select from.
                        type: string
                      name:
                        description: Name is the name of the Secret.
                        type: string
                      namespace:
                        description: Namespace of the Secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  value:
                    description: Value directly specifies the value as a plain string.
                    type: string
                type: object
              defaultScopes:
                description: |-
                  defaultScopes is the space-separated list of scopes requested during
                  authorization. Defaults to "openid" when omitted.
                type: string
              displayName:
                description: displayName is the human-friendly name shown in the login
                  form.
                type: string
              enabled:
                description: enabled controls whether the identity provider is active.
                type: boolean
              firstBrokerLoginFlowAlias:
                description: |-
                  firstBrokerLoginFlowAlias is the authentication flow used the first
                  time a user logs in through this identity provider.
                type: string
              hideOnLoginPage:
                description: hideOnLoginPage hides the identity provider from the
                  realm's login page.
                type: boolean
              issuer:
                description: issuer is the expected OIDC issuer identifier.
                type: string
              jwksURL:
                description: jwksURL is the JSON Web Key Set URL used to validate
                  provider signatures.
                type: string
              linkOnly:
                description: |-
                  linkOnly restricts the identity provider to account linking only,
                  preventing it from being used for direct authentication.
                type: boolean
              logoutURL:
                description: logoutURL is the OIDC end-session (logout) endpoint.
                type: string
              postBrokerLoginFlowAlias:
                description: |-
                  postBrokerLoginFlowAlias is the authentication flow executed after
                  every login through this identity provider.
                type: string
              realmRef:
                description: realmRef is a reference to the Realm this identity provider
                  belongs to.
                properties:
                  kind:
                    default: Realm
                    description: Kind specifies the kind of the Realm resource.
                    enum:
                    - Realm
                    type: string
                  name:
                    description: Name specifies the name of the Realm resource.
                    type: string
                  namespace:
                    description: Namespace specifies the namespace of the Realm resource.
                    type: string
                required:
                - name
                type: object
              storeToken:
                description: storeToken enables storing tokens issued by the external
                  provider.
                type: boolean
              syncMode:
                description: |-
                  syncMode controls when Keycloak imports/synchronizes user data from the
                  external provider (e.g. "IMPORT", "LEGACY", "FORCE").
                enum:
                - IMPORT
                - LEGACY
                - FORCE
                type: string
              tokenURL:
                description: tokenURL is the OIDC token endpoint.
                type: string
              trustEmail:
                description: |-
                  trustEmail trusts email addresses asserted by the external provider,
                  skipping Keycloak's own email verification.
                type: boolean
              userInfoURL:
                description: userInfoURL is the OIDC user info endpoint.
                type: string
              validateSignature:
                description: |-
                  validateSignature enables signature validation of tokens issued by the
                  external provider.
                type: boolean
            required:
            - alias
            - authorizationURL
            - clientID
            - clientSecret
            - realmRef
            - tokenURL
            type: object
          status:
            description: status defines the observed state of OIDCIdentityProvider
            properties:
              conditions:
                description: |-
                  conditions represent the current state of the OIDCIdentityProvider resource.
                  Each condition has a unique type and reflects the status of a specific aspect of the resource.

                  Standard condition types include:
                  - "Available": the resource is fully functional
                  - "Progressing": the resource is being created or updated
                  - "Degraded": the resource failed to reach or maintain its desired state

                  The status of each condition is one of True, False, or Unknown.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              keycloakID:
                description: keycloakID is the internal UUID assigned by Keycloak
                  for this identity provider.
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
